# Tiktok Live Web

React + TypeScript + Ant Design management UI for the live listener service.

```bash
pnpm install
pnpm dev
```

The development server runs on `http://localhost:5173` and proxies `/api` to
`http://localhost:8080`. Override the backend with `VITE_DEV_PROXY_TARGET`.
